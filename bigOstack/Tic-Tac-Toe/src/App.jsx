import { useState } from 'react'
import './App.css'

/**
 * This is a tic tac toe app
 * 
 * We are meant to have the boxes as buttons
 * The buttons are meant to have unique values which would be plotted against the winning matrix
 * We are meant to have the input state that toggles "X" and "O"
 * 
*/

function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      
    </>
  )
}

export default App
